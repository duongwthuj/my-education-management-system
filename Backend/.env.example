# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/LMS

# Power Automate Configuration
# Webhook URL để gửi email thông qua Power Automate
# Để lấy URL này, bạn cần:
# 1. Đăng nhập Power Automate (https://make.powerautomate.com)
# 2. Tạo flow mới với trigger "When a HTTP request is received"
# 3. Copy HTTP POST URL và paste vào đây
POWER_AUTOMATE_WEBHOOK_URL=

GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json
GOOGLE_SHEET_ID=1UVF-Gvw3M4LIS-EOwsdmCdpKBBD-oNHEEEp0kzpi6eg

# CORS Configuration (optional)
# CORS_ORIGIN=http://localhost:3000

# Rate Limiting (optional)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
